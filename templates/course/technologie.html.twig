{% extends 'layouts/default/default.html.twig' %}

{% block title %}{{ technology ? 'Technologie : '~ technology.name : 'Technologie non trouvée' }} - {{ parent() }}{% endblock %}

{% block body %}
    {% if technology|length == 1 %}
        <header class="container mx-auto mt-6 mb-12 pb-6 border-b border-gray-300 px-4 xl:mt-20 dark:border-gray-500">
            <div class="flex items-center mb-2">
                <img
                    data-controller="symfony--ux-lazy-image--lazy-image"
                    src="{{ asset('imgs/placeholder-75.svg') }}"
                    data-hd-src="{{ asset_image(asset('uploads/technologies/'~ technology.picture), { 'w': 85, 'h': 85 }) }}"
                    alt="{{ technology.name }}"
                    class="hidden rounded-full shadow mr-6 xl:inline"
                    height="85" width="85"
                >
                <div class="space-y-3">
                    <h1 class="text-4xl font-bold xl:text-5xl dark:text-white">
                        {{ technology.name }}
                    </h1>
                    <p class="text-lg sm:w-2/3 xl:w-full dark:text-gray-400">
                        {{ technology.description }}
                    </p>
                </div>
            </div>
        </header>
        {% include 'components/_cards.html.twig' with { 'courses': courses } only %}
    {% else %}
        <div class="mt-28">
            <p class="text-6xl font-black mb-6 dark:text-white">Ah oui ! Mais non... 😶</p>
            <p class="text-2xl text-gray-500 dark:text-gray-400">
                <a href="{{ path('course_index') }}"
                   title="Accueil" aria-label="Accueil"
                   class="text-blue-500 hover:text-blue-300 transition ease-in"
                >
                    Retournes à la page d'accueil
                </a>
            </p>
        </div>
    {% endif %}
{% endblock %}
